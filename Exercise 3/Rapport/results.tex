\chapter{Results}
\section{Determining the index of refraction of air}
At zero gauge pressure the velocity of the light is $v_1$, 
making the travel time over length $L$  equal to $t_1=\frac{L}{v_1}$ through the cylinder.
Increasing the pressure in the glass cylinder changes index of refraction,
leading to a new velocity $v_2$ and travel time $t_2 = \frac{L}{v_2}$.
The difference in time between the two pressures is 
\[\Delta t = t_2 - t_1 = L\frac{v_1 - v_2}{v_2v_1}\]
$v_1$ and $v_2$ can be expressed using the refractive indices 
$$v_i = \frac{c}{n_i},\quad i = 1,2.$$
If we now use equation \refeq{prop_to_pressure}, where $n_1 = 1 + \eta$ and $n_2 = 1 + \eta_2$
we get that $$\frac{\eta_2}{\eta} = \frac{p}{p_0} \iff \eta_2 = \frac{p}{p_0}\eta$$
where $p_0$ is the starting pressure (we will be using atmospheric pressure), and $p$ is the end pressure.
All this together finally gets us
\[\Delta t = \frac{L}{c}\frac{\frac{1}{1 + \eta} - \frac{1}{1 + \frac{p}{p_0}\eta}}{\frac{1}{1 + \frac{p}{p_0}\eta}\frac{1}{1 + \eta}} = (\frac{p}{p_0}-1)\frac{L}{c}\eta\]
This difference in time results in a difference in phase by the relation $\Delta \phi = \omega \Delta t = \frac{2\pi c}{\lambda} \Delta t$.
However we can also determine $\Delta \phi$ by counting the amount of periods $\Omega$, givin the relation $\Delta \phi = 2\pi \Omega$.
We can then get the final result by equating our two expressions for $\Delta \phi$:
\begin{align}
2\pi \Omega &= \frac{2\pi c}{\lambda}(\frac{p}{p_0}-1)\frac{L}{c}\eta \nonumber \\
\iff \eta &= \frac{\Omega}{(\frac{p}{p_0} - 1)}\frac{\lambda}{L} \label{eta equation}
\end{align}
We measure $\Omega$, $L$ and $p$, so the error on our measurement of $\eta$ is:
\begin{align}
  \sigma_\eta &= \sqrt{ \left( \frac{\eta}{\Omega}\sigma_\Omega \right)^2 + \left(\frac{\eta}{L}\sigma_L \right)^2 + \left(\frac{1}{p_0}\frac{\eta}{\frac{p}{p_0} - 1}\sigma_p \right)^2} \nonumber \\
  &= |\eta| \sqrt{ \left( \frac{\sigma_\Omega}{\Omega} \right)^2 + \left( \frac{\sigma_L}{L} \right)^2 + \left(\frac{1}{p_0}\frac{\sigma_p}{\frac{p}{p_0} - 1} \right)^2} \label{eta uncertainty}
\end{align}
The challenge when trying to get a correct value for $\eta$ was determining $\Omega$ because of the unkown change in frequency results in an unkown extra fraction of the easily countible peaks.
We have tried to account for this, by taking $\arccos$ to the first and last measurement, after norming the signal such that it is within -1 to 1, by subtracting the mean, and dividing by the amplitude.
The difference between the whole number of periods and the fractional can be seen in Figure 4. 
\begin{figure*}
    \centering
    \includesvg[width=0.95\textwidth]{../figures/Aircell.svg}
    \caption{The calculation of $n=1+\eta$ for the different datasets with uncertainty and a $95\%$ confidence interval. The upper is the whole number period approximation and the lower is where a fractional approximation is included.} 
    \label{fig:AircellFig}
\end{figure*}
Where we achieve the results seen in Tabel 1 from the weighted means, where the errors where assumed to be $\sigma_p = 0.1 \text{ bar}, \sigma_\Omega = 1$ and $\sigma_L = 0.5 \cdot 10^{-3} \text{ m}$.
\begin{table}[!ht]
    \caption{The results of the experiments with the aircell with the whole number approximation labelled as Method 1 and fractional as Method 2 with their uncertainties.}
    \centering
    \begin{tabular}{c c c}
        \hline\hline
        Method & $n$ & $\sigma_{n}$ \\
        \hline
        \rule{0pt}{1.1em}\noindent
        1 & 1.000204 & $5.2 \cdot 10^{-6}$ \\
        2 & 1.000214 & $5.4 \cdot 10^{-6}$ \\
        \hline
    \end{tabular}
    \label{table:AircellData}
\end{table}
\mbox{We notice a }value closer to the expected value of $n=1.00027717$ \autocite{air}. 
Though the calculated value still seems to be far off. The expected value is out of the $95\%$ confidence interval. \\
We think the offset could be explained by an uncertainty in the atmospheric pressure $p_{\textup{0}}$. 
This is because of the way our data was collected as explained in $1.2$. The data collection was started when the gauge pressure was seen to fall to $1 \textup{ bar}$ and stopped close to $0 \textup{ bar}$.
Because the gauge was only given in increments of $0.1 \textup{ bar}$ we would normally say that the uncertainty is only $0.05 \text{ bar}$, but because we were measuring it as it was changing, we will asumme our visual messurement had an uncertainty of $\sigma_{p} = 0.1 \textup{ bar}$.
We did however fail to consider that we did not always end the data collection at exactly $p_{\textup{0}} = 1 \textup{ bar}$ as assumed before. 
If we try to evaluate this error and change our $p_{\textup{0}}$ in the calculation of $\eta$ we get a value of $n$ within the confidence interval at $p_{\textup{0}} = 1.111 \textup{ bar}$ and an almost exact value of $n$ at $p_{\textup{0}} = 1.137 \textup{ bar}$.
This makes sense if we assume our uncertainty of $\sigma_{p} = 0.1 \text{ bar}$ also applies to $p_{\textup{0}}$.
\section{Test of intensity hypothesis}
In order to test the validity of equation \eqref{RetardedIntensity}, 
we will attempt to fit it to our data.
To do this, we will extract the maximum value of each intensity measurement,
knowing that this happens when $\cos(\Delta \phi) = \text{sgn}(\cos(2\theta))$.
We will therefore be fitting the function
\[I = I_0(1 + |\cos(2\theta)|) + b\]
where $b$ is added because of background radiation. 
This fit results in figure \ref{fig:WaveplateFit}.
When picking the maxima, we require that a certain amount of peaks have that maximal value, to prevent counting outliers.
\begin{figure*}
    \centering
    \includesvg[width=0.95\textwidth]{../figures/WaveplateFit.svg}
    \caption{}
    \label{fig:WaveplateFit}
\end{figure*}